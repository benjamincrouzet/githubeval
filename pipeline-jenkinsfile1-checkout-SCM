pipeline {
    agent any

    stages {
        stage('CheckoutSCM') {
            steps {
                cleanWs()
		}
	}
        stage('Clone GitHub') {
            steps {
                git branch: 'main', url: 'https://github.com/benjamincrouzet/githubeval.git'
            }
        }
    }
}